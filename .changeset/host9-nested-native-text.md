---
"@input/pen-dom": patch
---

A native text-entry control nested in the editor root keeps its focus (HOST9). Typing in host chrome such as an inline prompt no longer pulls the caret back into the field.
