---
"@input/pen-ai": patch
---

Proactive suggestions now replace only the suggestions inside the analyzed scope. Earlier sentences in the same block keep their underlines until an edit kills their range, so a paragraph can show more than one fix at a time.
