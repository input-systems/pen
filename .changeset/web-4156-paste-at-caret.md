---
"@input/pen-dom": patch
---

Place pasted blocks (parsed HTML and Markdown, and full blocks from the Pen clipboard) at the caret instead of after the caret's block: the first pasted block joins the text before the caret, the last joins the text after it, and blocks in between split the line.
